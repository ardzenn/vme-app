<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Chat - VME App</title>
    <%- include('partials/header-meta') %>
    <style>
        body { overflow: hidden; }
        .full-chat-container { display: flex; flex-direction: column; height: calc(100vh - 70px); background-color: var(--white); }
        .full-chat-header { display: flex; align-items: center; padding: 1rem; border-bottom: 1px solid var(--border-color); background-color: var(--white); }
        .full-chat-header img { width: 45px; height: 45px; border-radius: 50%; object-fit: cover; }
        .full-chat-header h4 { margin: 0 0 0 1rem; }
        .full-chat-messages { flex-grow: 1; overflow-y: auto; padding: 1rem; }
        .full-chat-input-area { padding: 1rem; border-top: 1px solid var(--border-color); background-color: #f8f9fa; }
    </style>
</head>
<body data-user-id="<%= currentUser._id %>">
    <%- include('partials/navbar') %>

    <div class="full-chat-container">
        <div class="full-chat-header" id="full-chat-header">
            <a href="/dashboard" class="btn btn-light me-3">&larr; Back</a>
            </div>

        <div class="full-chat-messages" id="full-chat-messages">
            <p class="text-center text-muted mt-5">Loading chat...</p>
        </div>
        
        <div id="full-typing-indicator" class="p-2 text-muted small" style="display: none; height: 30px; font-style: italic; padding-left: 1rem;"></div>

        <div class="full-chat-input-area">
            <form id="full-message-form">
                <div class="input-group">
                    <input type="text" id="full-message-input" class="form-control" placeholder="Type a message..." autocomplete="off" disabled>
                    <button type="submit" class="btn btn-primary" disabled>Send</button>
                </div>
            </form>
        </div>
    </div>

    <script src="/socket.io/socket.io.js"></script>
    <script src="/js/chat.js"></script> 
</body>
</html>
<!DOCTYPE html>
<html lang="en">
<head>
    <title>Forgot Password - VME App</title>
    <%- include('partials/header-meta') %>
    <style>
        .navbar { display: none; }
    </style>
</head>
<body>
    <div class="login-page">
        <div class="login-wrapper">
            
            <div class="login-form-container">
                <div class="text-center mb-5">
                    <img src="/images/logo.png" alt="VME Logo" style="height: 60px; margin-bottom: 1rem;">
                    <h1 style="font-size: 1.75rem;">Reset Your Password</h1>
                    <p class="text-secondary">Enter your email and we'll send you a link to get back into your account.</p>
                </div>

                <% if (locals.error_msg && error_msg.length > 0) { %>
                    <div class="alert alert-danger"><%= error_msg %></div>
                <% } %>
                <% if (locals.success_msg && success_msg.length > 0) { %>
                    <div class="alert alert-success"><%= success_msg %></div>
                <% } %>

                <form action="/forgot-password" method="POST">
                    <div class="mb-4">
                        <label for="username" class="form-label">Username (Email)</label>
                        <input type="email" class="form-control" id="username" name="username" required>
                    </div>
                    <button type="submit" class="btn btn-primary w-100 py-2">Send Reset Link</button>
                </form>

                <div class="text-center mt-4">
                    <a href="/login" class="text-secondary small">Back to Sign In</a>
                </div>
            </div>

            <div class="login-image-container">
                 <img src="/images/logo.png" alt="VME App Illustration" class="login-logo-img">
                 <h2 class="login-brand-title">Vintel Med Enterprises</h2>
            </div>
        </div>
    </div>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Forgot Password - VME App</title>
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>
    <div class="login-page">
        <div class="login-wrapper">
            <div class="login-form-container">
                <div style="max-width: 400px; margin: auto;">
                    <h2 style="font-size: 1.75rem; margin-bottom: 0.5rem;">Reset Your Password</h2>
                    <p style="margin-bottom: 2rem;">Enter your email and we'll send you a link to get back into your account.</p>
                    
                    <% if (error) { %><div style="background-color: #FEE2E2; color: #B91C1C; padding: 1rem; border-radius: 0.5rem; margin-bottom: 1.5rem;"><%= error %></div><% } %>
                    <% if (success) { %><div style="background-color: #D1FAE5; color: #065F46; padding: 1rem; border-radius: 0.5rem; margin-bottom: 1.5rem;"><%= success %></div><% } %>

                    <form action="/forgot-password" method="POST">
                        <div style="margin-bottom: 1.5rem;">
                            <label for="username" class="form-label">Username (Email)</label>
                            <input type="email" id="username" name="username" class="form-control" required>
                        </div>
                        <button type="submit" class="btn btn-primary" style="width: 100%; padding: 0.8rem;">Send Reset Link</button>
                    </form>

                    <p style="text-align: center; margin-top: 2rem; font-size: 0.875rem;">
                        <a href="/login" style="font-weight: 600; text-decoration: none;">Back to Login</a>
                    </p>
                </div>
            </div>
            <div class="login-image-container"></div>
        </div>
    </div>
</body>
</html>
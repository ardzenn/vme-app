<nav style="background-color: var(--white); border-bottom: 1px solid var(--border-color); padding: 1rem 2rem; display: flex; justify-content: space-between; align-items: center;">
    <div>
        <a href="/dashboard" style="font-weight: 700; color: var(--text-dark); text-decoration: none; font-size: 1.25rem;">VME App</a>
    </div>
    <div>
        <% if (user) { %>
            <span style="margin-right: 1.5rem; color: var(--text-light);">
                Hello, <strong><%= user.firstName %></strong> (<%= user.role %>)
            </span>
            <a href="/profile" class="btn btn-secondary btn-sm">Profile</a>
            <a href="/logout" class="btn btn-danger btn-sm" style="background-color: transparent; border-color: var(--danger); color: var(--danger);">Logout</a>
        <% } %>
    </div>
</nav>
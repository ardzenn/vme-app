<nav style="background-color: var(--white); border-bottom: 1px solid var(--border-color); padding: 0.75rem 2rem;">
    <div class="container-fluid">
        <a class="navbar-brand" href="/dashboard" style="display: flex; align-items: center; gap: 12px;">
            <img src="/public/images/logo.png" alt="VME Logo" style="height: 40px;">
            <span style="font-weight: 700; color: var(--text-dark); font-size: 1.1rem;">VINTEL MED ENTERPRISES</span>
        </a>
        
        <div>
            <% if (user) { %>
                <span class="d-none d-md-inline" style="margin-right: 1.5rem; color: var(--text-light);">
                    Hello, <strong><%= user.firstName %></strong> (<%= user.role %>)
                </span>
                <a href="/profile" class="btn btn-secondary btn-sm">Profile</a>
                <a href="/logout" class="btn btn-danger btn-sm" style="background-color: transparent; border-color: var(--danger); color: var(--danger);">Logout</a>
            <% } %>
        </div>
    </div>
</nav>
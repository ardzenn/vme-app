<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Reset Password - VME App</title>
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>
    <div class="login-page">
        <div class="login-wrapper">
            <div class="login-form-container">
                <div style="max-width: 400px; margin: auto;">
                    <h2 style="font-size: 1.75rem; margin-bottom: 0.5rem;">Enter a New Password</h2>
                    <p style="margin-bottom: 2rem;">Please choose a new, strong password.</p>
                    
                    <% if (error) { %><div style="background-color: #FEE2E2; color: #B91C1C; padding: 1rem; border-radius: 0.5rem; margin-bottom: 1.5rem;"><%= error %></div><% } %>

                    <form action="/reset-password/<%= token %>" method="POST">
                        <div style="margin-bottom: 1.5rem;">
                            <label for="password" class="form-label">New Password</label>
                            <input type="password" id="password" name="password" class="form-control" required>
                        </div>
                        <div style="margin-bottom: 1.5rem;">
                            <label for="confirmPassword" class="form-label">Confirm New Password</label>
                            <input type="password" id="confirmPassword" name="confirmPassword" class="form-control" required>
                        </div>
                        <button type="submit" class="btn btn-primary" style="width: 100%; padding: 0.8rem;">Reset Password</button>
                    </form>
                </div>
            </div>
            <div class="login-image-container"></div>
        </div>
    </div>
</body>
</html>